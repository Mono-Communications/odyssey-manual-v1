# 7-2. 세션 인증 실패

**증상**: 기동은 되지만 발송 시 `Auth Fail` / `Login Fail` 로그 반복

**점검**:

* KT 크로샷: `auth-file` 값과 `auth/` 폴더의 `.cert` 파일명이 **대소문자까지 정확히 일치**하는지 확인
* KT RCS / CPaaS: `rcs-id`/`rcs-secret` 또는 `api-id`/`api-pw` 발급값 확인
* 패스워드 암호화 적용 시 `common.user.otp`와 암호문 짝 확인
