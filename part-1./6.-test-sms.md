# 6. 테스트 발송 — SMS 1건

> **SCHEDULE\_TIME**, **SUBMIT\_TIME**의 포맷은 고객사 DB에 맞게 변경하세요.
>
> **수신번호(RCPT\_DATA)**, **발신번호(CALLBACK\_NUM)**&#xB294; 고객사 운영 환경에 맞게 변경하세요.

## <mark style="color:blue;">DBMS 별 DATE\_FORMAT</mark>

예시 쿼리는 MySQL 기준으로 작성되었습니다. 고객사 DBMS 별 <mark style="color:green;">**`schedule_time`**</mark>, <mark style="color:green;">**`submit_time`**</mark> 등의 컬럼에 대하여 insert 쿼리가 달라질 수 있습니다. 해당 포맷 설명을 참고하세요.

<table><thead><tr><th width="301">DBMS 종류</th><th>DATE_FORMAT</th></tr></thead><tbody><tr><td>MySQL, MariaDB</td><td><code>DATE_FORMAT(NOW(), '%Y%m%d%H%i%s')</code></td></tr><tr><td>Oracle, PostgreSQL</td><td><code>TO_CHAR(SYSDATE, 'YYYYMMDDHH24MISS')</code></td></tr><tr><td>SQL Server</td><td><code>FORMAT(GETDATE(), 'yyyyMMddHHmmss')</code></td></tr></tbody></table>

## <mark style="color:blue;">테스트 발송 쿼리</mark>

Odyssey 정상 기동 후, 다음 SQL을 실행하면 SMS 1건이 발송됩니다.

```sql
INSERT INTO ODYSSEY (
    MSG_TYPE, SCHEDULE_TIME, SUBMIT_TIME,
    MESSAGE, CALLBACK_NUM, RCPT_DATA
) VALUES (
    1,                                      -- 1 = SMS
    DATE_FORMAT(NOW(), '%Y%m%d%H%i%s'),     -- DBMS 별 포맷 확인 필요
    DATE_FORMAT(NOW(), '%Y%m%d%H%i%s'),     -- DBMS 별 포맷 확인 필요
    '[테스트] Odyssey 첫 발송 성공!',
    '0212345678',                           -- 발신번호 (특수문자 제외)
    '01012345678'                           -- 수신번호 (특수문자 제외)
);
```

## <mark style="color:blue;">결과 확인</mark> <a href="#id-1" id="id-1"></a>

발송 결과는 다음 3가지 채널 중 편한 곳에서 확인할 수 있습니다.

### <mark style="color:blue;">1. 로그</mark> <a href="#id-1" id="id-1"></a>

```bash
# 전체 로그 (모든 모드)
tail -f logs/live/system.log

# 특정 메시지 종류만 보기 (kt 크로샷 직접연동 문자)
tail -f logs/live/sender-kt-legacy.log

# 에러만 보기
tail -f logs/live/error.log
```

발송 및 로그 이관 성공 시 <mark style="color:green;">**`[KtSession-1] req_report 처리 customId=S****** result=0`**</mark> / **`[FetchPoller] archive 완료 count=1 time=104ms`** 등의 메시지가 출력 됩니다.

### <mark style="color:blue;">2. Web UI</mark> <a href="#id-2-web-ui-multi" id="id-2-web-ui-multi"></a>

[5. Web UI 살펴보기](5.-web-ui-multi.md)의 모니터링 대시보드 접속 시, 파이프라인 그래프·TPS에서 메시지가 처리되는 과정을 시각적으로 확인할 수 있습니다.

### <mark style="color:blue;">3. DB</mark> <a href="#id-3-db" id="id-3-db"></a>

발송이 완료되면 **`ODYSSEY`** 테이블의 **`STATUS`**&#xC640; **`RESULT`** 컬럼이 업데이트됩니다.

또한 수신자 휴대폰에 메시지가 도착했다면 실발송 성공입니다.

```sql
SELECT MSG_ID, STATUS, RESULT, RESULT_DESC, DELIVER_TIME
FROM ODYSSEY
WHERE MSG_TYPE = 1
ORDER BY MSG_ID DESC
LIMIT 10;
```

<table><thead><tr><th width="262">컬럼</th><th>의미</th></tr></thead><tbody><tr><td><strong><code>STATUS</code></strong></td><td><strong><code>2</code></strong> = 1차 전송 완료 (<a href="../part-3./18.-msg_type-status.md">18. MSG_TYPE / STATUS 코드</a> 참고)</td></tr><tr><td><strong><code>RESULT</code></strong></td><td>전송 결과 코드 (성공: <strong><code>0</code></strong>)</td></tr><tr><td><strong><code>RESULT_DESC</code></strong></td><td>결과 상세 메시지</td></tr><tr><td><strong><code>DELIVER_TIME</code></strong></td><td>발송 시각</td></tr></tbody></table>

{% hint style="info" %}
다른 메시지 타입(LMS / MMS / VMS / RCS / 카카오 알림톡 등)의 발송 쿼리는 [17. 메시지 타입별 INSERT](../part-3./17.-insert/)를 참고
{% endhint %}
